<div class="row course_tab">
  <div class="col-md-7">
    <a href="<%= course.url %>">
      <img class="img-fluid rounded mb-3 mb-md-0" src="<%= course.cover_source %>" alt="<%= course.url %>">
    </a>
  </div>
  <div class="col-md-5">
    <h3><%= course.title.titleize %></h3>
    <h4><%= course.instructor %></h4>
    <p><%= course.summary %></p>
    <a class="btn btn-primary" href="<%= course.url %>">View Course</a>
  </div>
</div>
<hr>